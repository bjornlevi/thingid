## ThingID environment example

# Used by Flask for sessions/CSRF-like features. Set a real random value in production.
FLASK_SECRET_KEY=change-me-really-do-it

# Optional URL prefix for running behind a reverse proxy (no trailing slash).
# Use this for both:
# - Nginx preserving the prefix upstream, or
# - Nginx stripping the prefix but sending `X-Forwarded-Prefix`.
THINGID_PREFIX=/thingid

# Optional DB URL
# Example: THINGID_DB=/srv/thingid/data/althingi.db
THINGID_DB=/srv/thingid/data/althingi.db

# Alternate DB URL form:
# DATABASE_URL=sqlite:////srv/thingid/data/althingi.db
